/**
 * ARQUIVO: frontend/next.config.js
 * * O Next.js requer que o arquivo de configuração principal seja '.js' ou '.mjs'.
 * Este arquivo configura o Next.js para a aplicação no diretório 'frontend'.
 * * @type {import('next').NextConfig}
 */
const nextConfig = {
  // Configurações básicas para o Next.js.
  // Como o projeto está no diretório 'frontend', esta configuração é padrão.
};

module.exports = nextConfig;
