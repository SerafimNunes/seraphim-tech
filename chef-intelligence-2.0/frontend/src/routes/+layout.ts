// Caminho: frontend/src/routes/+layout.ts

// Usamos esta função para passar dados acessíveis ao lado do cliente 
// para o layout.
export async function load() {
    // Retorna um objeto vazio por enquanto. 
    // Se precisássemos de dados globais públicos (ex: nome do ERP)
    // que não exigem a store, eles iriam aqui.
    return {};
}